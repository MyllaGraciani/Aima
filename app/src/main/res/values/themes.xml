<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Aíma" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/am_black</item>
        <item name="colorPrimaryVariant">@color/am_black</item>
        <item name="colorOnPrimary">@color/am_black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/am_black</item>
        <item name="colorSecondaryVariant">@color/am_black</item>
        <item name="colorOnSecondary">@color/am_black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Aíma.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Aíma.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Aíma.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>